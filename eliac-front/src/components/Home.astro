---
---
<div class="app-root">
      <header class="top-nav" aria-label="site navigation">
        <nav>
          <ul class="nav-list">
            <li>
              <a class="nav-link"
                href="https://innova-physics-upv.github.io"
                target="_blank"
                rel="noopener noreferrer">
                About us
              </a>
            </li>
            <li><a class="nav-link"  href="NormativaEliac.pdf" target="_blank" rel="noopener noreferrer ">Handbook</a></li>
          </ul>
        </nav>
      </header>

      <main class="content" role="main">
        <div class="site-grid">
          <h1>ELIAC</h1>
          <h2>European Linear Accelerator Challenge</h2>
          <div class="cta-wrap">
            <button class="cta" disabled>Enroll</button>
          </div>
        </div>
      </main>
    </div>


<style>
	* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background-color: white;
  color: black;
  font-family: Helvetica, Arial, sans-serif;
  height: 100vh;
  display: grid;
  place-items: center;
}

h1 {
  font-size: 4rem;
  font-weight: bold;
  margin-bottom: 1rem;
  letter-spacing: -0.02em;
}

h2 {
  font-size: 1.5rem;
  font-weight: normal;
  letter-spacing: 0.01em;
}

.site-grid{
  display: grid;
  grid-template-columns: 1fr auto;
  grid-template-rows: auto auto;
  gap: 0.5rem 2rem;
  align-items: center;
  max-width: 64rem;
  padding: 2rem;
}

.site-grid h1{
  grid-column: 1 / -1;
  font-size: 5rem;
  justify-self: center;
  line-height: 0.95;
}

.site-grid h2{
  grid-column: 1 / -1;
  justify-self: center;
  color: #111;
  opacity: 0.8;
}

@media (max-width: 640px){
  .site-grid{
    grid-template-columns: 1fr;
    gap: 0.25rem;
    padding: 1.25rem;
  }
  .site-grid h1{
    font-size: 3rem;
    text-align: center;
    grid-column: 1 / -1;
  }
  .site-grid h2{
    text-align: center;
    grid-column: 1 / -1;
  }
}

/* App layout */
.app-root{
  width: 100%;
  min-height: 100vh;
  display: grid;
  grid-template-rows: auto 1fr;
}

/* Top navigation (shad.cn-like minimal bar) */
.top-nav{
  height: 56px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid rgba(0,0,0,0.06);
  padding: 0 2rem;
}
.nav-list{
  list-style: none;
  display: flex;
  gap: 1.25rem;
}
.nav-link{
  text-decoration: none;
  color: inherit;
  font-weight: 500;
  padding: 0.25rem 0.5rem;
}
.nav-link:hover{
  background: rgba(0,0,0,0.04);
  border-radius: 4px;
}

/* Main content area centers the grid */
.content{
  display: grid;
  place-items: center;
  padding: 3rem 2rem;
}

.cta-wrap{
  grid-column: 1 / -1;
  justify-self: center;
  margin-top: 1.5rem;
}
.cta{
  background: black;
  color: white;
  border: none;
  padding: 0.75rem 1.25rem;
  font-size: 1rem;
  letter-spacing: 0.02em;
  border-radius: 6px;
  cursor: pointer;

}
.cta:hover{
  background: #111;
}
.cta:disabled {
  opacity: 0.8;
  cursor: not-allowed;
}

@media (max-width: 640px){
  .top-nav{ padding: 0 1rem; }
  .cta-wrap{ justify-self: center; }
  .cta{ width: 100%; max-width: 320px; }
}
</style>
